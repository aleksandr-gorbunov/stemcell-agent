OPENAI_API_KEY=sk-...

# Main stem agent model.
STEMCELL_MODEL=gpt-5.5

# Cheaper model used by `single_shot` inside agent-authored scripts.
STEMCELL_TOOL_MODEL=gpt-5-mini

# Model used inside a domain's verifier.py if it implements LLM-as-judge.
STEMCELL_VERIFIER_MODEL=gpt-5.5
