2026-05-15: train_dns_01 failed because detect_dns_tunnel.py computed `total` as the number of unique FQDN buckets instead of the true total query volume (sum of doc_count). The threshold (min-total=500) likely filtered real tunneling because unique names < 500 while total queries >= 500. Fixed by summing doc_count per FQDN and using that for `total`; kept entropy fraction based on unique left-most labels. Updated SKILL.md accordingly.